# Дальнейшие действия по системе Adaptive EV

Документ фиксирует **операционные шаги** строго в логике нашей системы (геометрическая балансировка, FSM, risk-first), отдельно для движения цены вверх и отдельно для движения вниз.

> Контекст по скриншоту: открыт симметричный замок BUY/SELL (по 0.10), текущая цена находится между уровнями, плавающий минус есть по обеим ногам. Дальше работаем только по правилам системы, без угадывания направления.

---

## Общие инварианты (действуют всегда)

1. Перед любым действием пересчитать:
   - `P_avg`,
   - `δ = S + α·ATR`,
   - survival: `δ·L_s > Δ·L_b`.
2. Любое частичное закрытие делать только при триггере:
   - `|P - P_avg| > Δ`.
3. Сохранять защиту SELL:
   - `L_s >= L_s,min`.
4. Ограничивать скорость изменения объёма:
   - не закрывать более установленного лимита за цикл.
5. При росте риска (просадка, нарушение survival, резкий рост спреда) переходить в STRESS.

---

## Сценарий A: Цена движется ВВЕРХ

### A1. Первичная реакция (FLOW)
1. Проверить, что цена ушла выше `P_avg` более чем на `Δ`.
2. Рассчитать отношение частичного закрытия `ΔL_s/ΔL_b` по формуле системы.
3. Выполнить микроребаланс:
   - частично закрыть SELL (`ΔL_s`),
   - частично закрыть BUY (`ΔL_b`) в согласованной пропорции,
   - переоткрыть SELL частично (согласно правилам стенда/модуля исполнения), чтобы не потерять «крышу».

### A2. Если рост плавный
1. Оставаться в FLOW.
2. Поддерживать стандартные `γ` и `Δ`.
3. На каждом цикле фиксировать микро-edge через `δ` и обновлять `P_avg`.

### A3. Если рост ускоряется (TREND_UP / STRESS)
1. Снизить `γ` (уменьшить агрессию закрытия, чтобы не сжечь структуру).
2. Увеличить `Δ` (реже входить в цикл, меньше шума).
3. Увеличить `δ` в пределах лимита (компенсация издержек в более жёстком рынке).
4. Проверять drawdown и survival на каждом шаге.

### A4. Критические условия (эскалация)
Переходить в HARD RESET, если:
- `DD > D_max`,
- repeated survival violation,
- `L_s` стремится к `L_s,min` и дальше защита не удерживается.

Действия HARD RESET:
1. Остановить новые циклы.
2. Сократить риск (в первую очередь перегруженную сторону).
3. Пересобрать якоря (`P_b/P_s`) и вернуться в INIT только после стабилизации.

---

## Сценарий B: Цена движется ВНИЗ

### B1. Первичная реакция (FLOW)
1. Проверить, что цена ушла ниже `P_avg` более чем на `Δ`.
2. Пересчитать `ΔL_s/ΔL_b`.
3. Выполнить микроребаланс по системе:
   - частично закрыть BUY (`ΔL_b`),
   - частично закрыть SELL (`ΔL_s`) в требуемом соотношении,
   - переоткрыть SELL частично для сохранения структуры и survival-инварианта.

### B2. Если снижение плавное
1. Продолжать циклы FLOW.
2. Поддерживать рабочие значения `γ`, `Δ`.
3. Фиксировать результат серией небольших циклов, не допуская исчезновения SELL.

### B3. Если падение ускоряется (TREND_DOWN / STRESS)
1. Уменьшить `γ`.
2. Увеличить `Δ`.
3. Увеличить `δ` в рамках риск-политики.
4. Включить более частую проверку спреда и ликвидности.

### B4. Критические условия (эскалация)
Переход в HARD RESET при тех же признаках:
- `DD > D_max`,
- нарушение survival,
- невозможность удержать `L_s >= L_s,min`.

Действия:
1. Заморозка новых циклов.
2. Сокращение перекоса.
3. Пересборка позиции и повторный вход только после нормализации режима.

---

## Практический чек-лист на каждый новый бар

1. Обновить `S`, `ATR`, `δ`, `P_avg`.
2. Проверить trigger: `|P-P_avg|>Δ`.
3. Если trigger=true — выполнить 1 цикл геометрической балансировки.
4. Проверить `survival`, `DD`, `L_s,min`.
5. При ухудшении — STRESS-профиль (`γ↓`, `Δ↑`, `δ↑`).
6. При критике — HARD RESET.

---

## Результат применения

При движении вверх и вниз система действует симметрично по логике risk-first:
- не угадывает рынок,
- не усредняется бесконечно,
- извлекает локальный edge циклически,
- при ухудшении условий переходит в защитные состояния FSM.
