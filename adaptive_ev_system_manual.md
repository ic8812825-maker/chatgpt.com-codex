# Адаптивная система поиска положительного EV (обновлённый мануал)

## 1. Назначение
Система предназначена для **адаптивного управления уже открытой симметричной BUY/SELL структурой** с целью:
- удерживать риск в формальных пределах,
- извлекать локальный edge за счёт `δ`,
- адаптировать параметры к режиму рынка (`FLAT/TREND/VOL/STRESS`).

Система **не предсказывает направление рынка** и не предполагает бесконечной устойчивости против монотонного тренда.

---

## 2. Основные переменные
- `L_b, L_s` — текущие объёмы BUY и SELL.
- `P_b, P_s` — опорные цены BUY/SELL.
- `P_t` — текущая цена.
- `S` — спред.
- `ATR` — прокси волатильности.
- `Δ` (`delta_step`) — шаг срабатывания цикла.
- `δ` — edge-смещение.
- `γ` — интенсивность частичного закрытия.
- `L_s,min` — минимально поддерживаемый SELL.
- `D_max` — максимум допустимой просадки в тестовом профиле.

---

## 3. Формулы
### 3.1 Центр структуры
```math
P_{avg}=\frac{L_bP_b+L_sP_s}{L_b+L_s}
```

### 3.2 Edge-параметр
```math
\delta = S + \alpha\cdot ATR
```

### 3.3 Условие выживания
```math
\delta\cdot L_s > \Delta\cdot L_b
```

### 3.4 Соотношение частичного закрытия
```math
\frac{\Delta L_s}{\Delta L_b}=
\frac{P-P_b-\frac{S}{2}+\delta}{P_s-P-\frac{S}{2}-\delta}
```

### 3.5 Proxy-метрика ожидания
```math
EV_{proxy}=\sum(\delta\cdot\Delta L_s)-Cost
```

---

## 4. FSM (боевой цикл)
1. **INIT**: открыть симметричный замок, зафиксировать `P_b/P_s`, лимиты риска.
2. **FLOW**: при `|P-P_avg|>Δ` выполнять частичное закрытие по ratio.
3. **ADAPT**: периодически обновлять `(δ,γ,Δ)` из локального поиска.
4. **STRESS**: при всплеске отклонения/волатильности снижать `γ`, повышать `Δ`, повышать `δ`.
5. **REBALANCE**: периодически пересобирать якоря `P_b/P_s`.
6. **HARD RESET**: при нарушении инвариантов — аварийное сокращение позиции.

---

## 5. Правила исполнения
1. **Триггер цикла**: `|P-P_avg|>Δ`.
2. **Ограничение скорости**: `ΔL_b <= min(γ, 0.12*L_b)`, `ΔL_s <= 0.12*L_s`.
3. **Защита SELL**: после действий всегда `L_s >= L_s,min`.
4. **Переоткрытие SELL**: частичное переоткрытие (`~92%` закрытого SELL) для сохранения структуры.
5. **Издержки**: учитывать как минимум компонент спреда в каждом цикле.
6. **Аварийная коррекция**: если survival-условие не выполнено, уменьшать `γ`, увеличивать `Δ` и `δ`.

---

## 6. Режимы рынка
- **FLAT**: базовые `γ`/`Δ`, максимум циклов.
- **TREND**: уменьшение `γ`, увеличение `Δ`, чтобы снизить чрезмерный расход объёма.
- **VOLATILE**: увеличение `δ`, умеренное снижение `γ`.
- **SHOCK/STRESS**: режим защиты с жёсткими ограничителями и приоритетом инвариантов.

---

## 7. Протокол тестирования (обязательный)
Перед любым применением прогонять мультисценарный тест:
- `FLAT_LOW_VOL`
- `TREND_UP_MODERATE`
- `TREND_DOWN_MODERATE`
- `VOLATILE_MEAN_ZERO`
- `SHOCK_REGIME`

Критерий принятия конфигурации: **PASS во всех сценариях** по правилам тестового стенда (`equity>0`, `survival_violations=0`, `max_drawdown<=D_max`).

---

## 8. Ограничения применимости
- Мануал описывает **контроллер позиции**, а не предсказатель направления цены.
- Результаты тестового стенда не эквивалентны гарантии прибыли на реальном рынке.
- Требуется отдельная валидация на реальных издержках (комиссии, проскальзывание, задержки, ликвидность).
