# 📘 Adaptive EV — полный мануал системы разруливания минусовых замков (FSM / risk-first)

## 0) Назначение документа
Этот документ — **полная рабочая спецификация** для практического использования системы Adaptive EV в двух слоях:
1. **Операционный слой** (что делать по шагам в рынке).
2. **Расчетный слой** (какие формулы и ограничения управляют решениями).

Система проектируется как **контроллер риска**, а не как «предсказатель рынка».

---

## 1) Концепция системы
Классический «замок» (BUY+SELL) сам по себе не создает устойчивое +EV после издержек.  
Adaptive EV решает другую задачу: **ограничить хвост риска и монетизировать управляемые колебания** за счет:
- конечных циклов,
- адаптивного коэффициента `k(t)`,
- ограничений по экспозиции и просадке,
- обязательного режима сброса `ESCAPE`.

Ключевая идея: **не бесконечное удержание, а управляемый цикл с формальными границами**.

---

## 2) Словарь переменных
- `Lb`, `Ls` — суммарные лоты BUY/SELL.
- `Pb`, `Ps` — средние цены входа BUY/SELL.
- `P` — текущая цена (`mid`/`bid`/`ask` в зависимости от формулы).
- `Δ` — шаг цикла (trigger-step).
- `S` — спред (в цене).
- `ATR` — оценка волатильности.
- `α` — чувствительность edge к волатильности.
- `δ = S + α*ATR` — edge-параметр.
- `k` — коэффициент асимметрии/интенсивности управления позицией.
- `k_min`, `k_max` — допустимый диапазон `k`.
- `E = |Lb-Ls|` — экспозиционный перекос.
- `Emax` — предел перекоса.
- `DD` — текущая просадка.
- `Dmax` — предельная просадка.
- `Ls_min` — нижняя граница поддержания SELL-двигателя.

---

## 3) Базовые формулы
### 3.1 Центр конструкции
\[
P_{avg} = \frac{Lb\cdot Pb + Ls\cdot Ps}{Lb+Ls}
\]

### 3.2 Survival-условие
\[
\delta \cdot Ls > \Delta \cdot Lb
\]
Если нарушено — модель должна переходить в `STRESS/ESCAPE`.

### 3.3 Экспозиция
\[
E = |Lb-Ls| \le E_{max}
\]

### 3.4 Нормированная волатильность
\[
v = ATR / \Delta
\]

### 3.5 Базовый адаптивный `k(ATR)`
\[
k_{atr}=k_{min}+(k_{max}-k_{min})\cdot e^{-\lambda\cdot v}
\]

### 3.6 Риск-модифицированный `k_adaptive`
`k_adaptive` дополнительно штрафуется по `E` и `DD`, затем клипуется в `[k_min, k_max]`.

---

## 4) FSM-архитектура
### STATE 1 — `LOCK_BUILD`
- Открытие базовой нейтральной структуры (обычно симметричной).
- Проверка валидности входных параметров/маржи.

### STATE 2 — `FLOW`
- Нормальный цикл управления:
  - частичные закрытия BUY/SELL в асимметрии,
  - ограниченные переоткрытия,
  - удержание `E <= Emax`.

### STATE 3 — `STRESS`
Вход при приближении к лимитам риска (`E`, `DD`, маржа, survival).  
Действия:
- снижение `k`,
- ускоренное сокращение риск-ноги,
- ограничение новых циклов.

### STATE 4 — `ESCAPE`
Жесткий защитный режим:
- фиксация/сброс структуры,
- запрет открытия новых циклов,
- возврат в `LOCK_BUILD` только после нормализации.

---

## 5) Правила переходов FSM
Рекомендуемые триггеры:
1. `ESCAPE`, если:
   - survival=FAIL,
   - маржинальная нагрузка > лимита,
   - просадка хуже `-Dmax`.
2. `STRESS`, если риск приближается к лимитам (например `>70%`).
3. `FLOW` — только при нормальном risk-profile.

---

## 6) Логика вкладки «Следующий шаг»
Для сценариев `Цена вверх на Δ` и `Цена вниз на Δ` рассчитываются:
- состояние FSM,
- действие по структуре (FLOW/STRESS/ESCAPE),
- рекомендуемый `k`,
- размер действия,
- частичное закрытие BUY/SELL,
- факт/направление/лот/цена нового цикла,
- стоп-условие.

Текущая реализация:
- Up-сценарий → новый цикл `SELL`,
- Down-сценарий → новый цикл `BUY`,
- размер действия ограничивается risk-cap по марже.

---

## 7) Логика вкладки «Новые позиции»
- Триггер открытия по пробою `Pavg ± Δ`.
- Явное направление:
  - вверх: `BUY`,
  - вниз: `SELL`.
- Лот открытия = `max(0.01, min(mode_cap, margin_cap))`.
- Цена открытия учитывает bias-поправку.
- Считаются маржа и свободная маржа после открытия.

---

## 8) Параметры по умолчанию и диапазоны
Практические стартовые диапазоны:
- `k_min=1.2`, `k_max=2.2`, `lambda≈1.0`.
- `Δ` под инструмент (обычно 80–150 пунктов в пересчете).
- `Emax`: 0.4–0.7 лота (в зависимости от депозита/плеча).
- `Dmax`: задается жестко от риск-профиля счета.
- `Ls_min`: не давать SELL-двигателю «исчезнуть».

---

## 9) Риск-контроль (обязательные инварианты)
1. Никогда не превышать лимиты маржи и просадки.
2. При `ESCAPE` не открывать новые циклы.
3. Не усреднять без явного FSM-разрешения.
4. Не допускать накопления перекоса `E` выше `Emax`.
5. Не отключать защитные формулы в Excel вручную.

---

## 10) Интерпретация результатов тестов
**PASS в симуляции** означает, что в рамках выбранной модели и параметров:
- правила survival выполнялись,
- ограничения риска не ломались критично,
- метрики доходности/просадки в допуске.

Это **не равно** гарантии реальной прибыли на реальном исполнении.

---

## 11) Ограничения модели
1. Нет полного execution-слоя (комиссия, проскальзывание, latency, глубина рынка).
2. Нет walk-forward/out-of-sample в текущем Excel-слое.
3. В экстремальных гэпах дискретная логика может запаздывать.
4. Требуется дисциплина оператора: ручные override повышают риск.

---

## 12) Чеклист перед применением
1. Обновить `Bid/Ask/mid/ATR`.
2. Проверить лимиты `Emax`, `Dmax`, маржу.
3. Проверить FSM-состояние.
4. Проверить вкладки «Следующий шаг» и «Новые позиции».
5. Если `ESCAPE` — сначала сброс, затем только перезапуск.

---

## 13) Финальный принцип
Adaptive EV — это **система ограничения риска с адаптивным управлением**, где:
- убыток ограничивается архитектурно,
- прибыль формируется как функция дисциплины цикла, а не как обещание «вечного плюсования».
