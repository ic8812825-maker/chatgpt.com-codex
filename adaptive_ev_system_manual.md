# 📘 Мануал: система разруливания минусовых замков (FSM)

Тип: **ограниченный стохастический хедж с принудительным reset**.

## 1) Идея
Классический лок не устраняет убыток, а только перераспределяет его. Эта система не пытается «пересидеть рынок», а управляет риском по циклам с верхней границей потерь (через `Emax`, `Dmax`, FSM-переходы).

## 2) Базовые формулы
- Результат структуры: `R = Lb(P-Pb) + Ls(Ps-P)`.
- Центр конструкции: `Pavg = (Lb*Pb + Ls*Ps)/(Lb+Ls)`.
- Экспозиция: `E = |Lb-Ls| <= Emax`.
- Ограничения асимметрии: `k in [k_min, k_max]`.

## 3) Режимы FSM
- **LOCK_BUILD**: открыть симметричный старт `BUY=SELL`.
- **FLOW**: частичные закрытия и циклическое переоткрытие прибыльной стороны.
- **STRESS**: снизить `k`, ускорить уменьшение риск-ноги, запретить агрессивные новые циклы.
- **ESCAPE**: жесткий/мягкий сброс (фиксация прибыли/убытка), затем reset в LOCK_BUILD.

## 4) Адаптивный коэффициент `k(t)`
Базовый `k` от волатильности:
`k(v)=k_min + (k_max-k_min)*exp(-lambda*v)`, где `v=ATR/Delta`.

Риск-коррекция: при росте `E`, `DD` и истощении SELL — `k` уменьшается и ограничивается `clip(k_min, k_max)`.

Практический диапазон: `k≈1.8..2.2`, `k_min≈1.2`, `k_max≈2.2`.

## 5) Условие “точки прибыльности”
Симметричная лок-схема сама по себе не имеет устойчивого +EV после издержек.
Используется контролируемый **micro-bias**:
- асимметрия шага `Delta_up/Delta_down`,
- асимметрия `k_up/k_down`,
- фильтры `ATR` и `spread`,
- дисциплина `ESCAPE`.

Формально: `E[Profit] = G_cycles + G_micro + G_exec - Cost > 0`.

## 6) Ключевые ограничения риска
- Всегда держать `E <= Emax`.
- При `DD >= Dmax` — переход в ESCAPE без исключений.
- Не допускать исчезновения SELL-двигателя (`Ls >= Ls_min`).
- Не усреднять и не “пересиживать” без FSM-сигнала.

## 7) Что реализовано в Excel-калькуляторе
- Расчет `Pavg`, survival/trigger, маржи и просадки.
- Рекомендации по следующему шагу (`FLOW/STRESS/ESCAPE`).
- Лист «Новые позиции»: расчет направления, лота, цены и маржи открытия.
- Адаптивный расчет `k(ATR)` и `k_adaptive` с ограничением по риску.

## 8) Честный вердикт
Это не «система вечной прибыли».
Это риск-контроллер: **максимальный риск ограничивается**, а прибыль является побочным эффектом корректного управления циклами, издержками и режимами FSM.
