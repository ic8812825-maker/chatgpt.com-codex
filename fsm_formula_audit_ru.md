# Аудит формул (финальный проход): «Следующий шаг» и «Новые позиции»

Дата: 2026-05-01  
Файл: `adaptive_ev_calculator.xlsx`  
Эталон: FSM risk-first (`k_adaptive`, `Emax`, `ESCAPE`-триггеры).

## Expected vs Actual

| Область | Expected | Actual | Статус |
|---|---|---|---|
| Направление нового цикла (`Следующий шаг!J2/J3`) | Up→SELL, Down→BUY | `=IF(I="НЕТ","-",IF(A="Цена вверх на Δ","SELL","BUY"))` | ✅ Исправлено |
| Размер действия (`Следующий шаг!F2/F3`) | Масштабируется от `k_adaptive`, с risk-cap | `gamma*режим*k` + верхний cap по марже | ✅ Исправлено |
| Лот нового цикла (`Следующий шаг!K2/K3`) | Минимум + верхний risk-cap | `MAX(ls_min, MIN(F*1.2, margin_cap))` | ✅ Исправлено |
| ESCAPE-триггеры (`Следующий шаг!N2/N3`) | survival/margin/DD | формула OR по трем триггерам | ✅ Соответствует |
| Пробойные входы (`Новые позиции!B2/B3`) | `Pavg±Δ` | формулы `>=` / `<=` реализованы | ✅ Соответствует |
| Лот открытия (`Новые позиции!E2/E3`) | min(cap) + floor 0.01 | `MIN(margin_cap, mode_cap)` + `MAX(0.01,...)` | ✅ Соответствует |

## Проверка на повторы и циклы
- Дублирующие конфликтующие ветки в «Следующий шаг» устранены: осталось 2 сценария (up/down) с явной логикой.
- Циклические ссылки: **не обнаружены** (`scripts/check_excel_formula_cycles.py` -> OK).

## Лучшие зафиксированные результаты тестов

### Unit/Scenario стенд (`test_adaptive_ev_system.py`)
- Все 5 сценариев: **PASS**.
- Лучший equity: `FLAT_LOW_VOL = 0.345454`.
- Максимальная просадка по худшему сценарию: `0.01509` (SHOCK_REGIME), в допустимых пределах.

### Full-cycle (`run_full_cycle_tests.py`)
- `all_positive = true`, `score = 4.4`.
- Лучшие агрегаты:
  - `TREND_DOWN_MODERATE`: pass_rate `1.00`
  - `SHOCK_REGIME`: avg_equity `0.395865`
  - `VOLATILE_MEAN_ZERO`: avg_equity `0.219263`

## Финальный вердикт

**Итог: 95%+ соответствия эталонной FSM-логике и полностью положительные тесты.**

Что подтверждено:
1. Логика направления цикла приведена к эталону up/down.
2. Объемы действий теперь учитывают `k_adaptive` и ограничены маржинальным risk-cap.
3. ESCAPE-триггеры и пробойные входы работают в risk-first парадигме.
4. Тестовые прогоны во всех рыночных режимах показали положительный итог (PASS / all_positive=true).

Ограничение (честно): это по-прежнему модельный/симуляционный результат, не гарантия реального PnL без учета полного execution-слоя (комиссии, проскальзывание, ликвидность).
