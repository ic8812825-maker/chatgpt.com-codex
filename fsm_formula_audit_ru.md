# Аудит формул (2-й проход): вкладки «Следующий шаг» и «Новые позиции»

Дата проверки: 2026-05-01  
Объект: `adaptive_ev_calculator.xlsx`  
Эталон: ваш FSM-подход (LOCK_BUILD/FLOW/STRESS/ESCAPE, контроль `k`, `Emax`, `Dmax`, risk-first).

## 1) Expected vs Actual — «Следующий шаг»

| Ячейка | Expected (FSM-эталон) | Actual (в файле) | Оценка |
|---|---|---|---|
| B2/B3 | Целевая цена = `P ± Δ` | `=Ввод!B12±Ввод!B18` | ✅ Соответствует |
| C2/C3 | Источник состояния FSM должен идти из расчетного ядра | `=Расчеты!B25` | ✅ Соответствует |
| D2/D3 | Для `ESCAPE` — сброс, для `STRESS` — снижение риска/k, иначе `FLOW` | `IF` по `C2/C3` | ✅ Соответствует |
| E2/E3 | Рекомендованный `k` должен брать адаптивный коэффициент | `=ROUND(Расчеты!B24,2)` | ✅ Соответствует |
| F2/F3 | Размер действия = 0 в ESCAPE, иначе база действия | `=IF(C="ESCAPE",0,ROUND(MAX(0.01,Ввод!B19*Ввод!B23),2))` | ⚠️ Частично: использует `γ*режим`, но без явной привязки к `k` |
| G2/H2 | При движении вверх SELL закрывается быстрее BUY | BUY=40%, SELL=60% от `F2` | ✅ Соответствует логике асимметрии |
| G3/H3 | При движении вниз BUY закрывается быстрее SELL | BUY=60%, SELL=40% от `F3` | ✅ Соответствует логике асимметрии |
| I2/I3 | Новый цикл только вне ESCAPE и при trigger | `AND(C<>"ESCAPE",Расчеты!B11="ДА")` | ✅ Соответствует |
| J2/J3 | Направление нового цикла должно быть режимно-явным | Всегда `SELL` при `I=ДА` | ❌ Расхождение: для down-сценария нужен `BUY` или режимный выбор |
| K2/K3 | Лот нового цикла: не меньше минимума и не больше risk-cap | `MAX(Ввод!B20,F)` | ⚠️ Частично: есть минимум, но нет верхнего risk-cap |
| L2/L3 | Цена нового цикла = целевая цена сценария | `=B2/B3` | ✅ Соответствует |
| N2/N3 | Stop-условие по survival/margin/DD => ESCAPE | `IF(FAIL survival OR margin>dlim OR dd>dmax)` | ✅ Соответствует |

## 2) Expected vs Actual — «Новые позиции»

| Ячейка | Expected (FSM-эталон) | Actual (в файле) | Оценка |
|---|---|---|---|
| B2/B3 | Триггер открытия по пробою `Pavg ± Δ` | `IF(Ввод!B12>=/<=Расчеты!B5±Ввод!B18)` | ✅ Соответствует |
| C2/C3 | Действие: открыть только при сигнале | `IF(B="ДА","ОТКРЫТЬ","ЖДАТЬ")` | ✅ Соответствует |
| D2/D3 | Направление сценарно-явное | `BUY` (up), `SELL` (down) | ✅ Соответствует |
| E2/E3 | Лот открытия: min(маржинальный cap, режимный cap), floor 0.01 | Реализовано через `MIN(...)` + `MAX(0.01,...)` | ✅ Соответствует |
| F2/F3 | Цена открытия с bias | Для BUY: `Ask-Δ*bias`, для SELL: `Bid+Δ*bias` | ⚠️ Частично: формула есть, но bias-поправка симметрична и может быть не рыночной для части брокеров |
| G2/G3 | Маржа открытия | `E*contract*price/leverage` | ✅ Соответствует |
| H2/H3 | Свободная маржа после открытия | `Equity - NewMargin` | ✅ Соответствует |
| I2/I3 | Комментарий risk-first | Текстовый контроль есть | ✅ Соответствует |

## 3) Повторения / дубли / риск рассинхронизации

1. В «Следующий шаг» `J2/J3` всегда отдает `SELL` при открытии цикла — это главный функциональный перекос относительно FSM-эталона по направлению.
2. В `F2/F3` (лист «Следующий шаг») размер действия не использует `k` напрямую (используется `γ*режим`), из-за чего адаптивный `k` слабо влияет на объем действия.
3. На текущем проходе циклических ссылок не обнаружено (`scripts/check_excel_formula_cycles.py` — OK).

## 4) Подробный вердикт

**Итог: 75–80% соответствия эталонной FSM-логике.**

Сильные стороны:
- Каркас FSM, ESCAPE-триггеры, `P±Δ`, пробойные открытия и маржинные ограничения реализованы корректно.
- Вкладки теперь компактные и без дублированных веток формул.
- Циклические формулы устранены.

Критичные расхождения:
- Направление нового цикла в «Следующий шаг» зафиксировано на `SELL`; это нарушает сценарность up/down.
- Объем действия в «Следующий шаг» не масштабируется напрямую от `k_adaptive`.

Рекомендации к следующему коммиту:
1. Исправить `J2/J3`: up->`SELL`, down->`BUY` (или выбор через знак `P-Pavg`).
2. Исправить `F2/F3`: включить множитель от `k_adaptive` (например, `F ~ base * k` с клипом).
3. Добавить верхний риск-ограничитель для `K2/K3` (по `Emax`/марже), а не только минимум.

