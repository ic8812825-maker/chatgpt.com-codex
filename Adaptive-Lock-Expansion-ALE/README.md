# Adaptive Lock Expansion (ALE)

Изолированный проект `Adaptive Lock Expansion (ALE)`, созданный внутри `work`.

## Ветка проекта

Для проекта должна использоваться только одна выделенная ветка: `ale`.
