# Подробная инструкция по использованию `adaptive_ev_calculator.xlsx`

## 1. Назначение файла
`adaptive_ev_calculator.xlsx` — это рабочий калькулятор по системе Adaptive EV, который:
- принимает параметры брокера/символа,
- принимает все открытые позиции,
- считает P/L каждой позиции,
- считает общую маржу и нагрузку на депозит,
- проверяет ключевые условия системы (`P_avg`, `δ`, survival, trigger),
- показывает следующий шаг при движении цены вверх/вниз на шаг `Δ`.

---

## 2. Структура книги
Книга состоит из 4 листов:
1. **Ввод** — все базовые параметры.
2. **Позиции** — таблица открытых сделок.
3. **Расчеты** — агрегированные показатели системы.
4. **Следующий шаг** — действия при +Δ / -Δ.

---

## 3. Лист «Ввод» (что и как заполнять)
Заполняются только значения в колонке **B**:
- `Брокер`, `Символ`, `Знаков после запятой`.
- `Размер контракта` (обычно 100000 для Forex).
- `Кредитное плечо`.
- `Баланс`, `Эквити`, `Свободная маржа`.
- `Текущая цена (mid)`, `Bid`, `Ask`.

**Автоматизация цены:**
- Bid рассчитывается автоматически: `mid - spread/2`.
- Ask рассчитывается автоматически: `mid + spread/2`.
- Если меняете `mid` или `спред`, Bid/Ask обновляются автоматически.
- `ATR`, `Alpha (α)`, `Шаг Δ`, `Gamma (γ)`, `Минимум SELL Ls,min`, `Предел просадки Dmax`.

По умолчанию выставлены базовые параметры из лучшего тестового профиля:
- `α=0.30`, `Δ=0.00028`, `γ=0.12`, `Ls,min=0.25`.

**Важно:**
- `Спред` считается автоматически как `Ask - Bid`.
- `Уровень маржи, %` считается автоматически через лист «Расчеты».

---

## 4. Лист «Позиции» (ввод открытых сделок)
Для каждой позиции заполните:
- **Лот указывается строго в формате `0.00` (только 2 знака после запятой).**
- `ID`
- `Активна (Y/N)`
- `Направление (BUY/SELL)`
- `Лот`
- `Цена открытия`
- `SL`, `TP` (если есть)
- `Комиссия`, `Своп`

Остальные поля считаются автоматически:
- `Текущая цена`:
  - для BUY используется `Bid`,
  - для SELL используется `Ask`.
- `Разница цены`:
  - BUY: `Current - Open`,
  - SELL: `Open - Current`.
- `P/L, USD` = `Разница * Лот * РазмерКонтракта + Комиссия + Своп`.
- `Маржа, USD` = `Лот * РазмерКонтракта * ТекущаяЦена / Плечо`.
- `Лот частичного закрытия` = `min(Лот*γ, Лот*0.5)`.
- `Действие`:
  - `ЧАСТИЧНО ЗАКРЫТЬ`, если `|Разница цены| >= Δ`.
  - иначе `ДЕРЖАТЬ`.

---

## 5. Лист «Расчеты» (контроль системы)
Ключевые блоки:
- `Сумма BUY лотов`, `Сумма SELL лотов`.
- `Средняя цена BUY`, `Средняя цена SELL`.
- `P_avg`.
- `δ = S + α*ATR`.
- Survival:
  - Левая часть: `δ*Ls`.
  - Правая часть: `Δ*Lb`.
  - Статус: `OK` / `FAIL`.
- Триггер цикла: `|P-P_avg|>Δ` → `ДА/НЕТ`.
- `Общий плавающий P/L`.
- `Общая маржа`.
- `Нагрузка на депозит, %`.

---

## 6. Лист «Следующий шаг»
Показывает две ветки:
1. **Цена вверх на Δ**
2. **Цена вниз на Δ**

По каждой ветке отображается:
- `ID Позиции` (автовыбор первой активной позиции нужного направления),
- `Для BUY/SELL` (метка рекомендации для BUY или SELL),
- целевая цена,
- направление шага,
- рекомендованный лот,
- действие (`ЧАСТИЧНАЯ РЕБАЛАНСИРОВКА` / `ОЖИДАТЬ`),
- объём частичного закрытия,
- флаг «Полностью закрыть?» (если высокий риск/нарушение условий).

---

## 7. Проверка корректности формул (проведена)
Проверены и подтверждены формулы:
- `Спред = Ask - Bid`.
- `P/L` по каждой позиции для BUY/SELL с разными ценами исполнения.
- `Маржа` по позиции через контракт, цену и плечо.
- `P_avg` через взвешенные цены BUY/SELL.
- `δ = S + α*ATR`.
- Survival `δ*Ls > Δ*Lb`.
- Trigger `|P-P_avg|>Δ`.
- Нагрузка на депозит `%` через общую маржу и баланс.

---

## 8. Порядок работы (каждый новый бар/тик)
1. Обновить в листе «Ввод»: `mid`, `Bid`, `Ask`, при необходимости `ATR`.
2. Обновить список открытых сделок на листе «Позиции`.
3. Проверить лист «Расчеты»:
   - survival,
   - trigger,
   - общая маржа,
   - нагрузка на депозит.
4. Перейти на лист «Следующий шаг» и выполнить действие по ветке +Δ/-Δ.
5. После исполнения обновить позиции и повторить цикл.

---

## 9. Важные замечания
- Калькулятор — инструмент принятия решений, а не гарант прибыли.
- Для реальной торговли добавляйте контроль проскальзывания и новостной волатильности.
- При `FAIL` по survival или критической нагрузке маржи переходите в защитный режим (уменьшение риска/частичное закрытие/пересборка).


## 10. Как обновить расчеты и «Следующий шаг» после изменений
После любого изменения входных данных (mid, спред, ATR, позиции, лоты):
1. В Excel нажмите **Formulas -> Calculation Options -> Automatic** (режим должен быть «Автоматически»).
2. Внесите изменения на листах «Ввод» и/или «Позиции».
3. Нажмите **Ctrl+Alt+F9** (полный пересчет книги) либо **F9** (пересчет).
4. Проверьте лист «Расчеты»: `survival`, `триггер`, `нагрузка`, `просадка`.
5. Перейдите на лист «Следующий шаг» — колонки `ID Позиции`, `Рекоменд. лот`, `Частично закрыть лот` пересчитаются автоматически.

Если значения не обновились:
- убедитесь, что расчет не стоит в режиме «Вручную»,
- убедитесь, что у активных позиций стоит `Y` и корректно заполнены `ID`, `Направление`, `Лот`.
