# Полный подробный мануал
## Система разруливания минусового замка через компрессию объёмов

> **Важно:** материал ниже носит исключительно образовательный и инженерно-методический характер. Это не инвестиционная рекомендация.

---

## 1) Суть системы

Это **не классический мартингейл** и **не обычный лок**.  
Система разруливает минусовой замок через **перенос объёма ближе к текущей цене** и контролируемый перевес защитной стороны.

Ключевые принципы:

1. Первый плюс `id2` не участвует в разруливании.
2. Разруливается только рабочий минусовой замок `id1/id3`.
3. Старый дальний убыточный ордер уменьшается частичными закрытиями.
4. Взамен открываются новые ордера ближе к рынку.
5. Геометрия замка сжимается.
6. В тренде вниз поддерживается SELL-перевес.
7. В тренде вверх поддерживается BUY-перевес.

Главная идея:

- не «оплачивать» убыток первым плюсом,
- а перестроить корзину так, чтобы средние цены BUY/SELL сблизились,
- и вся корзина закрылась при `PnL >= 0`.

---

## 2) Базовая схема (старт)

Открытие:

| ID  | Тип  | Лот |
|---|---|---:|
| id1 | BUY  | 1.00 |
| id2 | SELL | 1.00 |

Цена ушла вниз на 100 пунктов → `id2` закрыт в плюс.

Правило по `id2`:

- `id2` — неприкосновенный запас,
- в расчёты разруливания не входит,
- в логике перестройки корзины не используется.

Далее открывается новый SELL:

| ID  | Тип  | Лот |
|---|---|---:|
| id3 | SELL | 1.00 |

Рабочий замок для разруливания:

| ID  | Тип  | Лот |
|---|---|---:|
| id1 | BUY  | 1.00 |
| id3 | SELL | 1.00 |

---

## 3) Что именно разруливается

Разруливается только рабочая корзина:

- `id1 BUY`
- `id3 SELL`
- + новые рабочие ордера Big/Small

Цель:

- вывести корзину к состоянию `общий результат >= 0`,
- без учёта первого плюса `id2`.

---

## 4) Термины

- **Start BUY**: дальний проблемный `id1 BUY`.
- **Start SELL**: новый `id3 SELL` после фиксации первого плюса.
- **Big BUY**: крупный BUY ниже рынка в тренде вниз.
- **Small SELL**: малый SELL вместе с Big BUY (страховка перекоса).
- **Big SELL**: крупный SELL выше рынка в тренде вверх.
- **Small BUY**: малый BUY вместе с Big SELL.

---

## 5) Логика для тренда вниз

При движении цены вниз:

1. Открыть `Big BUY` на новом уровне.
2. Открыть `Small SELL` на том же уровне.
3. Частично закрыть `id1`.
4. Проверить суммарные объёмы.
5. Обеспечить условие `TotalBUY <= TotalSELL`.

Локально:

- `Big BUY > Small SELL`

Глобально после частичного закрытия `id1`:

- `TotalBUY <= TotalSELL` (предпочтительно `<`).

---

## 6) Почему это работает

Если открыть Big BUY и не сократить старый `id1`, система быстро переходит в опасный BUY-перекос.  
Поэтому каждый новый Big-блок сопровождается сокращением старого дальнего объёма.

Пример уровня 1:

- Было: `BUY=1.00`, `SELL=1.00`
- Добавили: `Big BUY=0.90`, `Small SELL=0.30`
- До закрытия `id1`: `BUY=1.90`, `SELL=1.30`
- Нужно закрыть минимум `0.60` по `id1`
- После закрытия: `BUY=1.30`, `SELL=1.30`

---

## 7) Параметры системы (базовый шаблон)

- `StartLot = 1.00`
- `ChannelStep = 100 пунктов`
- `MaxSkew = 10–25%`
- `MaxLevels = 3..5` (предпочтительно 3)

Сетка (пример):

- L1: `Big=0.90`, `Small=0.30`, `Close id1=0.60`
- L2: `Big=0.30`, `Small=0.15`, `Close id1=0.30`
- L3: `Big=0.20`, `Small=0.15`, `Close id1=0.00`
- L4: `Big=0.10`, `Small=0.10`, `Close id1=0.00`
- L5: `Big=0.05`, `Small=0.05`, `Close id1=0.00`

---

## 8) Формулы

### 8.1 Суммарные объёмы

Для тренда вниз:

- `TotalBUY = id1_remaining + Sum(Big BUY) + Sum(Small BUY)`
- обычно `Small BUY = 0`, значит:
- `TotalBUY = id1_remaining + Sum(Big BUY)`

- `TotalSELL = id3 + Sum(Small SELL)`

### 8.2 Условие безопасности

- `TotalBUY <= TotalSELL`
- предпочтительно `TotalSELL - TotalBUY = 5..25% StartLot`

### 8.3 Минимально необходимое закрытие id1

- `Close_id1_min = max(0, TotalBUY_before_close - TotalSELL_after_add)`

### 8.4 Закрытие с целевым защитным перевесом

- `Close_id1 = max(0, TotalBUY_before_close - TotalSELL_after_add + TargetSELLAdvantage)`

Ограничение:

- `Close_id1 <= id1_remaining_old`

---

## 9) Базовая таблица уровней (тренд вниз)

| Уровень | Big BUY | Small SELL | Закрытие id1 | id1 остаток | Total BUY | Total SELL | Перевес |
|---|---:|---:|---:|---:|---:|---:|---|
| 0 | —    | —    | —    | 1.00 | 1.00 | 1.00 | 0 |
| 1 | 0.90 | 0.30 | 0.60 | 0.40 | 1.30 | 1.30 | 0 |
| 2 | 0.30 | 0.15 | 0.30 | 0.10 | 1.30 | 1.45 | SELL +0.15 |
| 3 | 0.20 | 0.15 | 0.00 | 0.10 | 1.50 | 1.60 | SELL +0.10 |
| 4 | 0.10 | 0.10 | 0.00 | 0.10 | 1.60 | 1.70 | SELL +0.10 |
| 5 | 0.05 | 0.05 | 0.00 | 0.10 | 1.65 | 1.75 | SELL +0.10 |

---

## 10) Процентная модель (универсальная)

База:

- `StartLot = 100%`
- `Lot = StartLot * Percent / 100`

Пример `StartLot=0.10`, `Big_1=90%`:

- `0.10 * 90 / 100 = 0.09`

### Универсальная сетка процентов

- L1: `Big 90%`, `Small 30%`, `Close id1 60%`
- L2: `Big 30%`, `Small 15%`, `Close id1 30%`
- L3: `Big 20%`, `Small 15%`, `Close id1 0%`
- L4: `Big 10%`, `Small 10%`, `Close id1 0%`
- L5: `Big 5%`, `Small 5%`, `Close id1 0%`

Смысл:

- локально `Big_N >= Small_N`,
- глобально `TotalBUY <= TotalSELL`.

---

## 11) Примеры масштабирования

### 11.1 StartLot = 1.00

- L1: `Big=0.90`, `Small=0.30`, `Close=0.60`
- L2: `Big=0.30`, `Small=0.15`, `Close=0.30`
- L3: `Big=0.20`, `Small=0.15`, `Close=0.00`

### 11.2 StartLot = 0.10

- L1: `0.090 / 0.030 / 0.060`
- L2: `0.030 / 0.015 / 0.030`
- L3: `0.020 / 0.015 / 0.000`

### 11.3 StartLot = 0.01

- L1: `0.009 / 0.003 / 0.006`
- L2: `0.003 / 0.0015 / 0.003`
- L3: `0.002 / 0.0015 / 0.000`

---

## 12) Округление под брокера

Если `LotStep = 0.01`:

- `LotRounded = round(Lot / LotStep) * LotStep`

Практическое правило для тренда вниз:

- Big BUY — округлять вниз,
- Small SELL — округлять вверх,

чтобы не потерять SELL-защиту.

---

## 13) Правила входа в новый уровень

Новый уровень открывается только после прохождения полного шага `ChannelStep`.

Пример:

- Уровень 1: ещё -100 пунктов
- Уровень 2: ещё -100 пунктов
- Уровень 3: ещё -100 пунктов

Запрещено открывать «частые» промежуточные уровни — это ускоряет раздувание маржи.

---

## 14) Правила выхода из системы

Полный выход по корзине:

1. `BasketPnL >= 0`, или
2. средние BUY/SELL настолько сблизились, что закрытие даёт ноль/плюс.

При откате вверх после тренда вниз:

1. поэтапно фиксировать прибыль с Big BUY,
2. разгружать мешающие Small SELL,
3. при возможности закрывать остаток id1,
4. затем аккуратно разгружать id3,
5. после каждого шага пересчитывать баланс.

---

## 15) Запрещённые действия

Нельзя:

1. Открывать Big BUY без частичного закрытия `id1`.
2. Допускать устойчивый `TotalBUY >> TotalSELL` при тренде вниз.
3. Увеличивать Big-блоки на следующих уровнях.
4. Игнорировать `MaxSkew`.
5. Учитывать `id2` в рабочем разруливании.
6. Рано закрывать `id3` на продолжающемся снижении.
7. Работать без лимита уровней.

---

## 16) Зеркало для тренда вверх

Если рынок идёт вверх (зеркальный сценарий):

- `Big SELL > Small BUY`,
- после частичного закрытия старого SELL должно быть:
- `TotalSELL <= TotalBUY` (предпочтительно `<`).

Это полная симметрия логики вниз, только стороны меняются местами.

---

## 17) Псевдокод алгоритма

```text
if downtrend:
    while level <= MaxLevels and step_moved:
        open BigBuy[level]
        open SmallSell[level]

        calc TotalBUY_before_close
        calc TotalSELL_after_add

        CloseNeed = max(0, TotalBUY_before_close - TotalSELL_after_add + TargetSELLAdv[level])
        CloseNeed = min(CloseNeed, id1_remaining)

        close id1 by CloseNeed
        recalc TotalBUY, TotalSELL, Skew

        if Skew out of limit: stop adding levels

        if basket_pnl >= 0: close basket and exit
```

---

## 18) Чек-лист контроля на каждом уровне

Перед открытием уровня:

- есть ли полный шаг `ChannelStep`;
- не превышен `MaxLevels`;
- хватает ли маржи.

После открытия уровня:

- рассчитаны `TotalBUY/TotalSELL`;
- рассчитан `Close_id1_min`;
- выполнен `TargetSELLAdvantage`;
- соблюден `MaxSkew`.

Перед выходом:

- `BasketPnL >= 0`;
- после закрытия не остаётся опасного перекоса.

---

## 19) Итоговый вердикт по системе

Система математически состоятельна, если выполняются три обязательных условия:

1. Каждый новый Big-ордер сопровождается частичным сокращением дальнего проблемного ордера.
2. Суммарный баланс контролируется после каждого действия.
3. Есть жёсткий лимит уровней и лимит перекоса.

Итоговая формула смысла:

- **локально:** `Big > Small`,
- **глобально:** защитная сторона больше (`SELL>=BUY` вниз / `BUY>=SELL` вверх).

Именно это превращает систему в управляемую модель компрессии замка, а не в неограниченное наращивание риска.
