# Мануал системы разруливания минусового замка

## Через геометрию, перекос лотов и математическую компенсацию без выбора направления цены

## 1. Цель системы

Цель системы — не угадать направление цены, а построить такую структуру ордеров, при которой любое существенное движение цены создаёт прибыльную рабочую сторону.

Эта прибыль используется для:

1. частичного закрытия худшей минусовой позиции;
2. уменьшения общего объёма замка;
3. вывода корзины в ноль или небольшой плюс;
4. полного закрытия всех позиций.

Главная идея:

- Цена идёт вверх → зарабатывает BUY-перекос.
- Цена идёт вниз → зарабатывает SELL-перекос.
- Цена стоит → система не должна агрессивно открывать новые позиции.

## 2. Исходный пример

Дано:

- BUY 0.10, убыток -200 пунктов
- SELL 0.10, убыток -10 пунктов

Объёмы равны:

- BuyLots = 0.10
- SellLots = 0.10
- NetLots = 0.00

Но общий результат отрицательный:

- TotalLoss = -200 пунктов × 0.10 - 10 пунктов × 0.10
- TotalLoss = -21 условная единица пунктов-лота

Упрощённо:

- -200 × 0.10 = -20
- -10 × 0.10 = -1
- Итого = -21

Задача системы — создать дополнительные математические циклы, которые постепенно компенсируют эти -21.

## 3. Основные принципы

### Принцип 1. Нет прогноза

Система не спрашивает: «Куда пойдёт цена?»

Она строит уровни:

- вверх пошла — работает верхняя геометрия;
- вниз пошла — работает нижняя геометрия.

### Принцип 2. Работает не направление, а перекос

При равных объёмах (BUY 0.10 / SELL 0.10) движение цены почти не даёт восстановления, потому что прибыль одной стороны компенсируется убытком другой.

Чтобы появлялась рабочая прибыль, нужно BUY ≠ SELL.

Например:

- BUY 0.10
- SELL 0.15

Тогда при падении цены SELL зарабатывает быстрее, чем BUY теряет.

### Принцип 3. Прибыль не забирается отдельно

Любая прибыль рабочей сетки используется для уменьшения старого минуса.

Рабочая прибыль → закрытие части худшей позиции.

## 4. Термины системы

- **Основная позиция**: старый ордер, который имеет большой минус.
- **Локальная позиция**: вторая сторона замка.
- **Рабочая сетка**: дополнительные BUY/SELL позиции, которые открываются по геометрии.
- **Худшая позиция**: позиция или группа позиций с наибольшим текущим убытком.

Определение худшей стороны:

- WorstSide = BUY, если BuyProfit < SellProfit
- WorstSide = SELL, если SellProfit < BuyProfit

**Перекос лотов** — разница между BUY-объёмом и SELL-объёмом:

- NetLot = BuyLots - SellLots

Если:

- NetLot > 0 → BUY-перекос;
- NetLot < 0 → SELL-перекос;
- NetLot = 0 → нейтральный замок.

## 5. Главные параметры системы

- **BaseLot** = 0.10
- **Step** = 50 пунктов
  - Рекомендуемо: Step ≥ 2–3 средних спреда; Step ≥ локальный шум цены.
- **K** = 1.5
  - 0.10 × 1.5 = 0.15
- **MaxLevels** = 5
- **MaxTotalLot** = 0.60
- **PartialCloseLot** = 0.01
- **Reserve** = 10–20%
  - Пример: рабочая прибыль $10, Reserve = $2, на закрытие минуса можно $8.
- **BasketTarget** = 0 или небольшой плюс (например +$1)
- **MaxDrawdown** = 20–30%
  - После достижения лимита: новые ордера запрещены, система переходит в SAFE.

## 6. Центр замка

- Center = (BuyPrice + SellPrice) / 2

Пример:

- BuyPrice = 1.10000
- SellPrice = 1.09800
- Center = 1.09900

## 7. Геометрия уровней

Если Step = 50 пунктов = 0.00500 (условно для 5 знаков):

- Level 0 = Center
- Level +1 = Center + Step
- Level +2 = Center + 2 × Step
- Level +3 = Center + 3 × Step
- Level -1 = Center - Step
- Level -2 = Center - 2 × Step
- Level -3 = Center - 3 × Step

Пример (Center = 1.09900):

- +1 = 1.10400
- +2 = 1.10900
- +3 = 1.11400
- -1 = 1.09400
- -2 = 1.08900
- -3 = 1.08400

## 8. Суть перекоса на уровнях

На каждом уровне система создаёт пару ордеров:

- BUY_add
- SELL_add

Но объёмы разные:

- SmallLot = BaseLot
- BigLot = BaseLot × K

При BaseLot = 0.10 и K = 1.5:

- SmallLot = 0.10
- BigLot = 0.15

## 9. Чередование перекоса

Чтобы не зависеть от направления цены, перекос должен чередоваться.

Иллюстрация:

- Level +1: BUY 0.15 / SELL 0.10
- Level +2: BUY 0.10 / SELL 0.15
- Level +3: BUY 0.15 / SELL 0.10
- Level -1: SELL 0.15 / BUY 0.10
- Level -2: SELL 0.10 / BUY 0.15
- Level -3: SELL 0.15 / BUY 0.10

Более практично:

- при движении вверх активируется BUY-доминирующий блок;
- при движении вниз активируется SELL-доминирующий блок.

Без прогноза: цена сама доходит до уровня.

## 10. Формула прибыли перекоса

- ProfitSkew = D × |NetLot| × PointValue

Где:

- D — расстояние в пунктах;
- NetLot — разница объёмов BUY и SELL;
- PointValue — стоимость пункта на 1 лот.

Упрощённо:

- ProfitSkew = D × |NetLot|

Пример:

- BUY 0.10
- SELL 0.15
- NetLot = 0.05
- D = 100
- ProfitSkew = 100 × 0.05 = 5 пунктов-лота

## 11. Формула общего результата корзины

- BasketProfit = Σ ProfitBuy + Σ ProfitSell - Commission - SpreadCost - Swap

Упрощённо:

- BasketProfit = TotalBuyProfit + TotalSellProfit

Полное закрытие разрешено, если:

- BasketProfit ≥ BasketTarget

## 12. Формула закрытия части худшей позиции

Пусть:

- Pnet = доступная рабочая прибыль
- LossPerLot = убыток худшей позиции на 1.00 лот

Тогда:

- CloseLot = Pnet / LossPerLot
- С резервом: CloseLot = (Pnet - Reserve) / LossPerLot

Пример:

- Pnet = $12
- Reserve = $2
- Доступно = $10
- Худший BUY 0.10 = -$20
- LossPerLot = 20 / 0.10 = $200
- CloseLot = 10 / 200 = 0.05

Значит можно закрыть BUY 0.05.

## 13. Правило частичного закрытия

Разрешено только если:

- Pnet ≥ LossPart + Reserve

## 14. Основной алгоритм системы

1. Определить замок: BuyLots > 0, SellLots > 0, BasketProfit < 0.
2. Рассчитать центр:
   - Center = (AverageBuyPrice + AverageSellPrice) / 2
   - AverageBuyPrice = Σ(BuyPrice_i × BuyLot_i) / ΣBuyLot_i
   - AverageSellPrice = Σ(SellPrice_i × SellLot_i) / ΣSellLot_i
3. Построить уровни:
   - UpperLevel[n] = Center + n × Step
   - LowerLevel[n] = Center - n × Step
4. На уровне открыть перекосный блок:
   - верхний уровень: BUY BigLot + SELL SmallLot;
   - нижний уровень: SELL BigLot + BUY SmallLot.
5. Рассчитать прибыль перекоса: Pnet.
6. Закрыть часть худшей стороны.
7. Пересчитать корзину и метрики.
8. При BasketProfit ≥ BasketTarget закрыть все позиции.

## 15–17. Детальные примеры циклов

Логика примеров:

- При падении формируется SELL-перекос, и прибыль идёт на разгрузку BUY.
- При росте формируется BUY-перекос, и прибыль идёт на разгрузку SELL.
- Повторяющиеся циклы частичного закрытия уменьшают «тяжёлую» сторону (например BUY 0.10 → 0.09 → 0.07 → 0.04 → 0.00).

## 18. Таблица логики действий

- Цена выше верхнего уровня → открыть BUY Big + SELL Small.
- Цена ниже нижнего уровня → открыть SELL Big + BUY Small.
- Появилась рабочая прибыль → закрыть часть худшей позиции.
- BasketProfit ≥ Target → закрыть всё.
- TotalLot ≥ MaxTotalLot → запрет новых ордеров.
- Drawdown ≥ MaxDrawdown → SAFE MODE.
- Цена в мёртвой зоне → не открывать новые позиции.

## 19. Мёртвая зона

- DeadZone = Center ± Step

Пример:

- Center = 1.09900
- Step = 50 пунктов
- Верхний триггер = 1.10400
- Нижний триггер = 1.09400

Пока цена внутри 1.09400–1.10400 — новых открытий нет.

## 20. Динамический шаг

- Step = ATR × M

Пример:

- ATR = 40
- M = 1.5
- Step = 60 пунктов

## 21. Формула лота

1. **Постоянный перекос**:
   - SmallLot = BaseLot
   - BigLot = BaseLot × K

2. **Линейный рост**:
   - SmallLot[n] = BaseLot
   - BigLot[n] = BaseLot × (1 + n × A)

3. **Геометрический рост**:
   - BigLot[n] = BaseLot × K^n

Практически безопаснее постоянный/линейный вариант, геометрический — агрессивнее.

## 22. SAFE MODE

Включается, если:

- TotalLots ≥ MaxTotalLot, или
- DrawdownPercent ≥ MaxDrawdown, или
- MarginLevel ≤ MinMarginLevel, или
- Spread > MaxSpread.

В SAFE MODE:

1. новые ордера запрещены;
2. разрешены только частичные закрытия;
3. разрешено полное закрытие корзины;
4. возможно ручное управление.

## 23. Ключевые формулы

- Center = (AverageBuyPrice + AverageSellPrice) / 2
- AverageBuyPrice = Σ(BuyPrice_i × BuyLot_i) / ΣBuyLot_i
- AverageSellPrice = Σ(SellPrice_i × SellLot_i) / ΣSellLot_i
- UpperLevel[n] = Center + n × Step
- LowerLevel[n] = Center - n × Step
- SmallLot = BaseLot
- BigLot = BaseLot × K
- NetLot = BuyLots - SellLots
- ProfitSkew = Distance × |NetLot| × PointValue
- BasketProfit = ΣProfitBuy + ΣProfitSell - Costs
- CloseLot = (Pnet - Reserve) / LossPerLot
- LossPerLot = |PositionLoss| / PositionLot
- PartialCloseAllowed = Pnet ≥ LossPart + Reserve
- FullCloseAllowed = BasketProfit ≥ BasketTarget

## 24. Рекомендуемые стартовые параметры

Для замка 0.10 / 0.10:

- BaseLot = 0.10
- K = 1.3–1.5
- Step = 50–100
- MaxLevels = 3–5
- MaxTotalLot = 0.50–0.70
- PartialCloseLot = 0.01
- Reserve = 10–20%
- BasketTarget = 0 или +$1
- MaxDrawdown = желательно не выше 20–30%

Консервативно:

- BaseLot = 0.05
- K = 1.5
- Step = 70–100

Агрессивно:

- BaseLot = 0.10
- K = 1.7–2.0
- Step = 50

## 25. Что система делает при любом движении

- Цена вверх → верхняя геометрия → BUY-перекос → прибыль гасит SELL-минус.
- Цена вниз → нижняя геометрия → SELL-перекос → прибыль гасит BUY-минус.
- Цена стоит → нет новых блоков, ожидание выхода из dead zone.

## 26. Важное предупреждение

Это не магическая гарантия выхода из любого замка. Эффективность зависит от волатильности, депозита, издержек (спред/комиссия/своп), ограничений лота и рыночных разрывов.

Корректно: система не предсказывает движение, а использует любое достаточное движение для создания перекоса и компенсации убытка.

## 27. Заключение

Это инженерная модель восстановления замка без выбора направления цены:

- геометрия уровней;
- двусторонние блоки;
- перекос лотов;
- частичное закрытие худшей позиции;
- контроль риска;
- выход по BasketProfit.

Отличие от обычной сетки: не просто усреднение, а управляемый перекос для удаления старого минуса.

Отличие от мартингейла: лот не растёт бесконечно; есть MaxLevels, MaxTotalLot, MaxDrawdown и SAFE MODE.

## 28. Вердикт

Концепция рабочая как инженерная модель восстановления замка **только при строгих лимитах**.

Ключевые риски:

- без лимитов система опасна;
- маленький Step уничтожает шум/спред;
- большой K перегружает объём;
- без частичных закрытий накапливается хаос;
- без SAFE MODE возможен слив депозита.

Оптимальная версия:

- Step по ATR;
- K = 1.3–1.5;
- частичное закрытие от 0.01;
- BasketTarget = 0 или малый плюс;
- жёсткий MaxTotalLot;
- обязательный SAFE MODE.

Итоговая формула:

**Любое движение цены → создаёт перекосную прибыль.**
**Перекосная прибыль → закрывает худший минус.**
**Худший минус уменьшается → замок становится легче.**
**Корзина выходит в 0/плюс → закрываем всё.**
