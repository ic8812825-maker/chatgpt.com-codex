# Split Geometry Changelog

## Current change

- Added hard `OnInit()` blocker for incomplete SplitGeometry builds.
- Added `PositionRole` and MT5-safe role comment build/parse helpers.
- Added split lot helpers and rounded split geometry validation helpers.
- Added universal multi-position lifecycle net scaffold for split Big-harvest accounting.
- Added additional split states, pending mappings, persistence fields and documentation.
