# Evidence index — четвёртая коррекция 3.1.5

`baseline.log` содержит воспроизведённые обходы. `causal_audit.log`, `source_guard.log`, `final_validator.log` получены непосредственным исполнением. `standalone_failure_manifest.txt` содержит точный baseline-набор 10 failures. `verification.log` содержит итоги полной проверки до независимого fresh-clone gate.
