# Неподтверждённые наборы

`BigScenario_Best_1.set` имеет статус:

```text
UNVERIFIED
НЕ ИСПОЛЬЗОВАТЬ В PRODUCTION
НЕ ИСПОЛЬЗОВАТЬ ДЛЯ ДОКАЗАТЕЛЬСТВА
NOT_FOR_MT5_TESTING
```

Файл не подтверждён accepted selectable CSV row и сохранён только для аудита происхождения. Возврат в основной каталог разрешён только после воспроизводимой регенерации и успешного валидатора.
